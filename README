# Node App
This is a simple app that creates and retrives a bunch of courses in a Mongo DB database.

It was created as a demo app for Kubernetes deployment along with MongoDB service in a single node for training.